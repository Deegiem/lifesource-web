import { platformAuditLogs } from "@/features/super-admin/audit-data";

export async function getPlatformAuditLogs() {
  // Replace with GET /audit-logs when the backend is ready.
  return platformAuditLogs;
}
